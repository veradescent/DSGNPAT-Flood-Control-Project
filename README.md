# DSGNPAT-Flood-Control-Project
1. Ensure that node.js is installed on your machine
```
node -v
```
2. Install the following dependencies, which will be used to read input, parse a CSV file and output a CSV file
```
npm install readline csv-parse csv-stringify
```
3. Execute the main file containing the program
```
node Main.js
```
